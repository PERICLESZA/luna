class Banck:
    def __init__(self, 
                id, name, agency, account, createdAt, updateAt):
                self.id = id,
                self.name = name,
                self.agency = agency,
                self.account = account,
                self.createAt = createdAt,
                self.updateAt = updateAt
                
    